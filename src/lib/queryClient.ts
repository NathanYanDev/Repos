// Imports and creates a new instance of `QueryClient` from `@tanstack/react-query` to manage server state and caching in the application.
import { QueryClient } from "@tanstack/react-query";

export const queryClient = new QueryClient();
