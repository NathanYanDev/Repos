import { SquarePlus } from "lucide-react";

export const BtnAdd = () => {
	return (
		<button type="submit" className="hover:opacity-10">
			<SquarePlus strokeWidth={1.5} size={32} />
		</button>
	);
};
